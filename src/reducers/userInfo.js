


const InitState = {
    userInfo: {}
}


export const userInfo = (state = InitState, action) => {
    let { type, data } = action
    switch(type) {
        
        default:
            return state
    }
}